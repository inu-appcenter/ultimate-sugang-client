# 03 §8 env 구조 — 실제 값은 .env 에 사람이 입력한다(.env 는 커밋 금지, 하네스가 생성하지 않음)
# 어댑터: mock(기본, D1) | http(계약 Q-1 도착 후)
VITE_API_ADAPTER=mock
# http 어댑터 선택 시에만 필수
VITE_API_BASE_URL=
# session → SESSION_EXPIRED 강제 발생(수동 검증용). 미설정이면 정상 동작
VITE_MOCK_FAIL=
